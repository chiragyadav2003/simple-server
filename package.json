{
  "name": "simple-server",
  "version": "1.0.0",
  "description": "a simple backend server to get random todos,calculate sum, subtraction, multiplication and simple interest",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "chirag yadav",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
